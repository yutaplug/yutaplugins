version = "1.0.1"
description = "Allow users to set a custom avatar frame. Shares API with Vencord."

aliucord {
    changelog.set(
        """
        # 1.0.1
        * Modernize settings interface.
        * Improve account status and authorization guidance.
        
        # 1.0.0
        * Initial plugin release!
        """.trimIndent(),
    )
}
