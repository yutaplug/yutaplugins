version = "1.0.0"
description = "Backports Decor custom avatar decorations to Discord 126.021."

aliucord {
    changelog.set(
        """
        # 1.0.0
        * Backport Decor avatar decorations to Discord 126.021.
        * Add system-browser authorization and per-account Decor tokens.
        """.trimIndent(),
    )
}
