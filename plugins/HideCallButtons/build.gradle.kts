version = "1.1.0"
description = "Hides the video and call buttons"

aliucord.changelog.set("""
# 1.1.0
* Added independent settings for hiding call buttons on each page
* Added the DM-only `/call` command
""")
