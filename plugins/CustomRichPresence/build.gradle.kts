version = "1.0.0"
description = "Set a custom Discord Rich Presence from Aliucord settings."

aliucord {
    changelog.set(
        """
        # 1.0.0
        * Initial plugin release!
        """.trimIndent(),
    )
}
