version = "1.0.1"
description = "Backports Discord's newer Markdown formatting to chat messages."
aliucord {
    changelog.set(
        """
        # 1.0.1
        * Fix forum posts
        """.trimIndent(),
    )
}