version = "1.0.0" // Plugin version. Increment this to trigger an update
description = "Fork of MoreHighlight" // Plugin description that will be shown to user

aliucord {
    changelog = """
        # 1.0.0
        * Initial version
    """.trimIndent()

    // Image or GIF that will be shown at the top of the changelog page
    //changelogMedia = "https://nice.png"

    author("yutaplug", 0L)

    // Uncomment if the plugin is unfinished
    //deploy = false
}
