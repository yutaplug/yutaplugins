version = "1.0.2"
description = "Backports Discord's evolving Nitro and experimental profile badges to mobile profile sheets."

aliucord {
    changelog.set(
        """
        # 1.0.2
        * Prioritizes the current catalog tier, handles numeric family IDs, and removes duplicate adapters.

        # 1.0.1
        * Uses the badge catalog for owned current tiers and prevents duplicate native/backported badges.

        # 1.0.0
        * Adds evolving Nitro, gifting, account age, streaming, game time, and game variety badges to profile sheets.
        """.trimIndent(),
    )
}
